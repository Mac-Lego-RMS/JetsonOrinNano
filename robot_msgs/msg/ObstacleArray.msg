# The COMPLETE current set of detected obstacles -- republished in full
# whenever anything changes, so a consumer just replaces its list.

std_msgs/Header header        # frame_id = 'map' (start-anchored)
Obstacle[] obstacles