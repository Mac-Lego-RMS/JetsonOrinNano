# Other

Everything else needed to understand how the vehicle is prepared for the
competition: datasets, hardware specifications, protocol descriptions.

Candidates for this folder:

- [ ] **UART protocol Jetson ↔ ESP32-S3** — implemented in
      [`../src/esp_bridge/`](../src/esp_bridge); the code refers to a
      `JETSON_BRIDGE.md` that does not exist in this repository yet
- [ ] part list / bill of materials
- [ ] notes on the YOLO training dataset (how it was captured and labelled)
- [ ] setup notes: Docker image, ROS 2 packages, how to build the workspace
